# TailLeader environment variables

# Optional: explicit path to YAML config
TL_CONFIG=/home/pi/tailleader/tailleader/config.yaml

# Optional: secure system control endpoints (/api/restart_service, /api/restart_pi)
# Leave disabled for normal use.
TL_ENABLE_SYSTEM_CONTROLS=false
TL_ADMIN_KEY=change-me
