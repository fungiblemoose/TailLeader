server:
  host: 0.0.0.0
  port: 8088
poller:
  interval_seconds: 10
  disappearance_seconds: 600
feeder:
  mode: file
  path: /run/adsbexchange-feed/aircraft.json

# Optional admin controls for restart/reboot APIs.
# Keep disabled unless you intentionally need these endpoints.
admin:
  enable_system_controls: false
  api_key: ""
