services:
  tailleader:
    build: .
    ports:
      - "8088:8088"
    restart: unless-stopped
    environment:
      - TL_CONFIG=/app/config.yaml
    volumes:
      - ./data:/app/data
      - ./config.yaml.example:/app/config.yaml:ro
